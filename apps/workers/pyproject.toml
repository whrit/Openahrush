[project]
name = "semrush-workers"
version = "0.1.0"
description = "Openahrush Workers - Background job processing and orchestration"
requires-python = ">=3.12,<3.13"
license = { text = "AGPL-3.0-or-later" }
authors = [{ name = "whrit" }]

dependencies = [
    "redis>=5.0",
    "pydantic-settings>=2.0",
    "semrush-core",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=4.0",
    "fakeredis>=2.20",
]

[project.scripts]
semrush-worker = "semrush_workers.runner:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/semrush_workers"]

